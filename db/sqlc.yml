version: "2"

sql:
  - engine: "postgresql"
    schema: "sql/schema.sql"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "database"
        out: "../internal/database"
        emit_json_tags: true
        emit_interface: true
