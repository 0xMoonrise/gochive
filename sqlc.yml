version: "2"
sql:
  - engine: "postgresql"
    queries: "migratons/query.sql"
    schema: "migrations/schema.sql"
    gen:
      go:
        emit_json_tags: true
        package: "database"
        out: "internal/database"
