version: "2"
sql:
  - engine: "postgresql"
    queries: "migrations/querys/querys.sql"
    database:
      uri: postgresql://db_user:db_password@127.0.0.1:5432/archive?sslmode=disable
    gen:
      go:
        emit_json_tags: true
        package: "database"
        out: "internal/database"
